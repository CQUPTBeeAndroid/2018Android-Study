<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zhangfan.work6">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.AllActivity" />
        <activity android:name=".Activities.IosActivity" />
        <activity android:name=".Activities.AndroidActivity" />
        <activity android:name=".Activities.ResourceActivity" />
        <activity android:name=".Activities.VideoActivity" />
        <activity android:name=".Activities.WelFareActivity" />
        <activity android:name=".Activities.AppActivity" />
        <activity android:name=".Activities.AheadActivity" />
        <activity android:name=".Activities.RecommentActivity" />
        <activity android:name=".WebActivities.WebActivity" />
        <activity android:name=".Activities.TimeActivity" />
        <activity android:name=".Activities.ImageActivity"></activity>
    </application>

</manifest>